import { SectionHeading } from "@/components/ui/SectionHeading";
import { SectionWrapper } from "@/components/ui/SectionWrapper";
import { achievements } from "@/lib/data/achievements";

export function AchievementsBlock() {
  return <SectionWrapper id="achievements"><div className="w-full space-y-12"><SectionHeading eyebrow="Recognitions" title="People, roles, and milestones" description="Placeholder recognition entries showing the person name, their status or job title, and the related achievement." /><div className="grid gap-6 sm:grid-cols-2 md:grid-cols-3">{achievements.map((item) => <article key={item.title} className="rounded-3xl border border-border bg-bg-elevated/75 p-6 shadow-[0_0_0_1px_rgba(255,255,255,0.02)] transition-transform duration-300 hover:-translate-y-1"><div className="flex items-start justify-between gap-4"><p className="font-mono text-xs uppercase tracking-[0.22em] text-accent">{item.year}</p><span className="rounded-full border border-accent/30 bg-accent/10 px-3 py-1 font-mono text-[11px] uppercase tracking-[0.2em] text-accent">Recognition</span></div><h3 className="mt-5 text-xl font-bold text-foreground">{item.title}</h3><div className="mt-5 space-y-3 border-t border-border pt-5"><div><p className="text-xs uppercase tracking-[0.22em] text-muted/80">Name</p><p className="mt-1 text-sm font-medium text-foreground">{item.name}</p></div><div><p className="text-xs uppercase tracking-[0.22em] text-muted/80">Status</p><p className="mt-1 text-sm text-muted">{item.status}</p></div><p className="pt-2 text-sm leading-7 text-muted">{item.details}</p></div></article>)}</div></div></SectionWrapper>;
}
